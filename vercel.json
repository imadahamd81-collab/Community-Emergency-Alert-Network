{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && mkdir -p api/public && cp -r frontend/dist/. api/public/",
  "outputDirectory": "api/public",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  }
}