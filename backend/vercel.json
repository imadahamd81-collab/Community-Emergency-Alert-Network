{
  "version": 2,
  "buildCommand": "cd ../frontend && npm install && npm run build && cd ../backend && mkdir -p api/public && cp -r ../frontend/dist/. api/public/",
  "outputDirectory": "api/public"
}